apiVersion: v1
kind: Service
metadata:
  name: 12-factor-python-app-service
spec:
  selector:
    app: 12-factor-python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer